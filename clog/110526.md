# 11/05/26

## 10:xx — compare.py: head-to-head model comparison (n=10, seed=42)

### Setup
- `compare.py` smoke-tested and fixed (llama-4-scout returned `reasoning` as a list; coerced to string)
- Replaced decommissioned `deepseek-r1-distill-llama-70b` with `meta-llama/llama-4-scout-17b-16e-instruct`
- Retrieval shared across models; only verification call varies

### Results (docs/compare_10_42.json)

| Claim | Gold | 3.3-70b | 4-scout | Both correct? |
|---|---|---|---|---|
| 1 — Gimenez pay raise | Supported | NEE ✗ | Supported ✓ | No |
| 2 — McMurray tax vote | Supported | NEE ✗ | NEE ✗ | No (retrieval: 1 item) |
| 3 — NZ Food Bill | Refuted | Refuted ✓ | NEE ✗ | No |
| 4 — Section 230 vote | Refuted | NEE ✗ | NEE ✗ | No |
| 5 — Charlotta Bass | Supported | Supported ✓ | Supported ✓ | Yes |
| 6 — Nigeria GDP 2020 | Supported | NEE ✗ | Supported ✓ | No |
| 7 — Trump/1994 lawsuit | Refuted | NEE ✗ | NEE ✗ | No |
| 8 — India-China imports | Refuted | NEE ✗ | NEE ✗ | No |
| 9 — Antifa wildfire | Refuted | Refuted ✓ | NEE ✗ | No |
| 10 — Marcos/Rizal | Refuted | Refuted ✓ | Refuted ✓ | Yes |

**llama-3.3-70b: 4/10 (40%) | llama-4-scout-17b: 4/10 (40%)**

### Key finding: opposite blind spots

The models tie at 40% but diverge on 4 claims (1, 3, 6, 9). The divergence is directional:

- **Llama 4 Scout commits to Supported more readily** — gets claims 1 (Gimenez) and 6 (Nigeria GDP) where 3.3-70b abstains with NEE. Lower threshold for positive assertion.
- **Llama 3.3-70B commits to Refuted more readily** — gets claims 3 (NZ Food Bill) and 9 (Antifa wildfire) where 4-scout abstains with NEE. More willing to call something false from limited evidence.

4-scout: better at confirming supported facts. 3.3-70b: better at flagging refuted claims.

### Retrieval failures dominate

Claims 2, 4, 7, 8 both models get wrong — all genuine retrieval failures (1 item, 2 items, Wikipedia 429 errors). These are not model failures; both models are correctly uncertain. These same claims failed in Baseline 1.

Wikipedia 429 rate limiting hit 4 consecutive times (claims 7–10). Backoff/retry is still needed.

### Implication: simple ensemble could outperform either model alone

Take Scout's verdict when it says Supported; take 70B's verdict when it says Refuted; default to NEE when both agree. This would flip claims 1, 3, 6, 9 to correct — potentially 8/10 = 80% on this sample. Worth testing.

---

## Session G: self-consistency + eval results tracking

### Changes
- `pipeline/verification.py`: Added `verify_claim_sc(claim_evidence, n=3)` — runs `verify_claim` N times, averages Likert scores per label, uses averaged float max to determine verdict. Vote summary prepended to reasoning string.
- `evaluate.py`: Added `--approach` flag (`baseline` / `fire` / `sc`). Checkpoint path includes approach to avoid run conflicts. Results dict extended with `post_confidence` and `source`. CSV logging appended to `docs/eval_results.csv` (run-level) and `docs/eval_claims.csv` (per-claim) at end of each run.
- `docs/plot_results.py` (new): Reads eval_results.csv, produces three plots — overall accuracy by approach, per-label accuracy (latest run per approach), accuracy over time.
- Added `pandas` and `matplotlib` to project dependencies.

---

## Session H: --verifier flag + qwen3/gpt model comparison runs (n=50)

### Changes
- `evaluate.py`: Added `--verifier` CLI flag to override `VERIFICATION_MODEL` at runtime without touching config. Patches `pipeline.verification.VERIFICATION_MODEL` and `pipeline.iterative.VERIFICATION_MODEL`. Verifier name threaded through checkpoint paths and run_id in CSV logging.
- `pipeline/verification.py`: `/no_think` appended to user content when verification model contains "qwen" (suppresses Qwen3 chain-of-thought tokens per IDEA-010 finding).
- `pipeline/iterative.py`: Same `/no_think` suppression in `_generate_followup_query`.

### Planned runs (n=50, seed=42, approach=fire)
```
uv run python3 evaluate.py --n 50 --seed 42 --verifier qwen/qwen3-32b
uv run python3 evaluate.py --n 50 --seed 42 --verifier openai/gpt-oss-120b
```

### Results — SC 200-claim run (seed=0)
- **122/200 = 61%** overall (vs FIRE 30/50 = 60%, seed=42 — different sample, not controlled)
- Refuted: 67% | Supported: **62%** | NEI: 60% | CE: n/a (none in sample)
- Key finding: Supported recall jumped from 43% (FIRE/seed=42) → 62% (SC/seed=0). Consistent with SC reducing false NEI on well-supported claims.
- Refuted recall dropped slightly: 71% → 67%. Model is marginally more cautious across 3 runs.
- Comparison is confounded by different seeds. Need same-seed FIRE vs SC run for a controlled test.

### Calibration research context
- Logprobs unavailable on Groq — logit-based confidence blocked.
- Best black-box approaches: invert-softmax + temperature scaling (needs ~300 labeled pairs to fit T), NLI consistency (no data needed, add DeBERTa).
- Temperature scaling deferred until sufficient eval_claims.csv data accumulated.
- Self-consistency first because it's the only method that can also improve verdict accuracy (not just calibration).

---

## 11:xx — SC 200-claim post-mortem (seed=0)

### Per-label breakdown (gold → predicted)

| Gold | n | correct | recall | dominant error |
|---|---|---|---|---|
| Refuted | 123 | 82 | 66.7% | 35× NEI (28% of class) |
| Supported | 55 | 34 | 61.8% | 11× NEI, 6× Refuted, 4× CE |
| NEI | 10 | 6 | 60.0% | 4× Refuted |
| Conflicting Evidence | 12 | 0 | **0.0%** | 6× Refuted, 5× NEI, 1× Supported |

Overall: 122/200 = **61%**. ECE = 0.3455 (severe overconfidence — mean post_confidence 0.95 vs accuracy 0.61).

### Comparison to FIRE baseline (30/50 = 60%, seed=42)

| Metric | FIRE (n=50, s=42) | SC (n=200, s=0) | Δ |
|---|---|---|---|
| Overall acc | 60% | 61% | +1pp (n.s., different seeds/n) |
| Refuted recall | 71% | 67% | −4pp |
| Supported recall | 43% | 62% | **+19pp** |
| NEI fraction of errors | 14/20 = 70% | 51/78 = 65% | −5pp |
| CE recall | — | 0% | unchanged failure |

**SC didn't move overall accuracy** but reshaped the error profile: ~half the Supported claims that FIRE incorrectly punted to NEI are now correctly Supported. Refuted recall slips slightly — averaging 3 runs makes the verifier marginally more cautious on borderline-refuted claims. Net wash on accuracy, but more *useful* errors (commits to a verdict instead of abstaining).

### NEI over-prediction: still the dominant failure mode

- 57/200 (28.5%) predictions are NEI; only 10/200 (5%) gold-NEI.
- 51/57 NEI predictions are wrong. 35 are Refuted claims, 11 Supported, 5 CE.
- The verifier defaults to NEI when it can't extract a clean signal from retrieved evidence — retrieval is doing the work, not the verdict aggregation. SC averages over Likert scores from the *same* evidence set, so it can't recover from retrieval failure.

### Conflicting Evidence: 0% recall

Of 12 CE-gold claims: 6 predicted Refuted, 5 NEI, 1 Supported, 0 CE. Predictions to CE: only 6 total across the whole sample, and all 6 were wrong-class. The verifier essentially never selects CE.
Likely cause: averaging 3 runs' ConflictingEvidence Likert scores rarely makes CE the argmax — Supported/Refuted/NEI scores dominate. The verdict-selection rule (argmax of averaged Likert) systematically suppresses CE.

### Vote-distribution analysis: not possible from current logs

`verify_claim_sc` builds `vote_str` (e.g. "Refuted×2, NEI×1") and prepends it to the reasoning string, but `eval_claims.csv` only stores `post_verdict` / `post_confidence` / `correct` — reasoning is dropped. **Action item:** if vote-distribution analysis is wanted, evaluate.py needs to persist reasoning (or at least extract and log the SC vote tuple) into eval_claims.csv. Not doing this now per session constraint (no SC-logic changes).

### Implications

1. **NEI is the bottleneck, not verifier disagreement.** SC at n=3 doesn't help where retrieval gives the verifier nothing to commit on. Improving retrieval (BM25 hybrid, IDEA-008) likely dominates further SC tuning.
2. **The argmax-of-averaged-Likert rule kills CE.** Worth testing majority-vote-of-verdicts as an alternative aggregation (cheap to add, doesn't change verify_claim itself).
3. **Calibration is broken** — confidence ~0.95 with 0.61 accuracy, ECE 0.35. Temperature scaling on the 200-claim labeled set is now viable; this is a higher-leverage next step than another SC variant.
4. **Same-seed FIRE-vs-SC** still needed for a clean Δ. Current comparison straddles two seeds and two sample sizes.

---

## Session: pipeline_slides.tex — 15-slide Beamer presentation

- Wrote `docs/pipeline_slides.tex` (metropolis theme, XeLaTeX, 16:9)
- 15 slides: title, goal, pipeline overview (TikZ), claim extraction, evidence retrieval, FIRE verification, Likert rubric, research foundations (4 arXiv links), misleadingness detection (IDEA-009), AVeriTeC 10-claim results, larger-scale results, CE recall problem+fix, fast shipping scenario (~$1–2K/month, 100 users), wide deployment (20K users, HPC/caching required), open questions
- Cost estimates: fast shipping $900–1800/month; wide at API pricing $150–230K/month → self-hosted/HPC required; 80% cache hit rate gives 5× reduction

---

## Session: validated pipeline description + updated pipeline_report.tex

- Validated user's end-to-end description of the pipeline against source code (claim_extraction.py, retrieval.py, iterative.py, verification.py, aggregation.py)
- Confirmed description is accurate; noted: MMR can return <5 items (early-stop at score threshold), Serper date-ceiling prevents future-evidence leakage, confidence threshold in aggregation is 0.65 (not 0.50 as the old .tex had)
- Rewrote `docs/pipeline_report.tex` to reflect current implementation: verifying questions in Step 1, BM25+rerank+MMR in Step 2, FIRE loop + Likert rubric in Step 3, correct aggregation threshold in Step 4

---

## 10:02 — Session 3: ECE implementation

### Changes
- `docs/plot_results.py`: `ece(confidences, corrects, n_bins=10)` (manual, no sklearn) + `plot_reliability_diagram()` (bar per bin, diagonal, per-bin sample counts). Self-test on dummy data passes.
- `evaluate.py`: `run_row` now logs `ece_score` right after `accuracy`, computed from per-claim `post_confidence`/`match`.
- `docs/eval_results.csv`: Backfilled `ece_score` for the existing 200-claim SC run from `eval_claims.csv` rather than re-running (data was already present).

### Result
- SC 200-claim run: **ECE = 0.3455** at accuracy = 0.61 — confirms the "calibration is broken" finding above with a concrete number for future calibration layers to beat.
- Reliability diagram saved to `docs/reliability_diagram.png`.

### Note
- Concurrent edit added `auroc()` + `plot_roc_curve()` to the same file; preserved.

---

## Session: CLAUDE.md cleanup — archived resolved research ideas (IDEA-005–010)

Ideas below have been incorporated into the Key Architectural Decisions table and the updated pipeline description. Full rationale preserved here.

### [IDEA-005] Multi-hop QA-driven retrieval (ClaimCheck / HerO 2 style)
*Added: 080526 after literature sweep on 2025-2026 SOTA*

Instead of 3 pre-formulated search queries per claim, the pipeline first generates **5–8 targeted verifying questions** from multiple angles (factual, contextual, source-attribution, contradicting evidence). For each question, evidence is retrieved and a direct answer is extracted. The verifier reasons over structured Q&A pairs rather than raw scraped snippets.

**Empirical support:** ClaimCheck (arxiv 2510.01226, NAACL 2025) achieves 76.4% on AVeriTeC dev set with Qwen3-4B, outperforming GPT-4o. Our current pipeline gets ~50% on the same benchmark. The gap is pipeline architecture, not model quality.

**Why it works:** Each question isolates a specific dimension of the claim's truth value. Structured Q&A is far easier for the verifier to reason over than noisy scraped paragraphs.

---

### [IDEA-006] FIRE-style iterative confidence-gated retrieval
*Added: 080526*

Instead of fixed N-query retrieval, use an agent loop: retrieve → assess confidence → if confident, output verdict; if uncertain, generate a targeted follow-up query → repeat. Early termination when (a) confidence exceeds threshold, or (b) the new query would be redundant with a prior query.

**Empirical support:** FIRE (arxiv 2411.00784, NAACL 2025 Findings) achieves comparable accuracy with 7.6× fewer LLM calls and 16.5× fewer search calls. For Tier 3 (async, no user-facing latency pressure), early-exit on easy claims offsets additional iterations on hard ones.

**Implementation notes:** Max iterations = 5. Redundancy check via cosine similarity between new query embedding and previous query embeddings; if > 0.9, skip. Confidence estimated from entropy of the verifier's p_supported / p_refuted distribution.

---

### [IDEA-007] Document summarization + answer reformulation before verification (HerO 2 style)
*Added: 080526*

Two preprocessing steps added before the verifier receives any evidence:
1. **Document summarization:** Each retrieved page is summarized into a claim-relevant excerpt. Removes noise, enforces temporal anchoring on the evidence text rather than on the model's parametric knowledge.
2. **Answer reformulation:** Evidence excerpts are rephrased into answer-form text ("This evidence supports / refutes the claim because…"), making the verifier's reasoning task structurally cleaner.

**Empirical support:** HerO 2 (arxiv 2507.11004, AVeriTeC 2025) identifies these as two of its four key contributions. Document summarization directly addresses our "date sensitivity" failure (Claim 5 in the evaluation: model used its own knowledge of 2024 statistics instead of the 2020 evidence).

---

### [IDEA-008] Hybrid retrieval stack: BM25 + dense + cross-encoder reranking
*Added: 080526*

Replace dense-only retrieval with a three-stage stack:
1. **BM25 (lexical):** Strong recall for named entities, numbers, exact quotes — fills gaps where dense retrieval underperforms.
2. **Dense (semantic):** Existing MiniLM embeddings.
3. **Cross-encoder reranking:** Merge candidate sets from both, rerank with a cross-encoder (e.g., `cross-encoder/ms-marco-MiniLM-L6-v2`) before passing top-K to the verifier.

**Why it matters:** Our "Nigeria 52% urban" failure (Claim 5) is a pure BM25 problem. A lexical match on "Nigeria urban population 2020" would have surfaced a 2020-vintage source instead of letting the model fall back to its parametric 2024 knowledge.

---

### [IDEA-009] Misleadingness detection via 4-step claim-stripping algorithm
*Added: 080526*

A formal algorithm for detecting posts that are **technically true but misleading**. Two categories:

**Misleading by scope:** A post makes a broad factual claim but only provides a narrow supporting instance (one study, one example, one time period). "A study shows vaccines cause autism."

**Misleading by implication:** A true statement A naturally suggests a false implication B for a reasonable reader. Often correlation → causality. "Cities with more immigrants have higher crime rates" (implies immigrants cause crime). Also covers misleading by omission: the statement is true only under specific circumstances that are not stated.

**4-step detection algorithm (applied per claim A):**
1. Establish A is likely true, preserving all qualifiers (time period, sample size, population, study count, expert count).
2. Strip all qualifiers → Ã. If Ã is likely false → **misleading by scope.**
3. Derive implication B: the first factual inference a reasonable person would draw from A. If B is likely false → **misleading by implication.**
4. Augment A with circumstances a reasonable person would silently assume → A⁺. If A⁺ is likely false → **misleading by omission.**

**Implementation:** Applied in Tier 3 after explicit claim verification. Steps 2–4 each generate a new derived claim (Ã, B, A⁺) that is fed through the standard retrieval + verification sub-pipeline. A misleading verdict increases the post's overall falsity score even when the literal claim is Supported.

**Why this matters:** None of the SOTA benchmark systems (ClaimCheck, FIRE, HerO 2) address misleadingness — they target stated factual claims only. Detecting "true but misleading" content is our primary differentiator for social media misinformation.

---

### [IDEA-010] Likert per-label confidence + MMR diversity reranking (CTU AIC)
*Added: 080526 — CTU AIC won AVeriTeC 2025 shared task on combined evidence+verdict metric*

**Likert per-label confidence:** Instead of a single confidence float, the verifier rates each label independently on a 1–5 scale (1=Strongly disagree, 5=Strongly agree) for all four classes: Supported, Refuted, Not Enough Evidence, Conflicting Evidence. Verdict with highest score wins. Exposes near-ties (e.g., Refuted=4, Conflicting=3) that a scalar collapses.

**MMR diversity reranking:** After retrieval + cross-encoder relevance ranking, apply Maximal Marginal Relevance to diversify the final evidence set. CTU AIC used k=40 retrieved chunks → MMR with λ=0.75 (relevance) / 0.25 (diversity) → l=10 passed to verifier. Prevents the verifier from receiving 10 rephrased versions of the same source. Recommended stack: cross-encoder first (relevance), MMR second (diversity).

**Negative finding — thinking tokens:** Ablation showed extended reasoning tokens performed on par with standard output. Token budget is better spent on retrieval coverage than in-context chain-of-thought. Do not use thinking mode in the verifier.

**Metric note:** CTU AIC's 33.17% / 0.50 AVeriTeC score and ClaimCheck's 76.4% are not the same metric. AVeriTeC score combines label accuracy + evidence quality. ClaimCheck's figure is label accuracy only. CTU AIC won the official task; ClaimCheck is the strongest on raw label accuracy.
