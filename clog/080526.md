# 08/05/26

## 14:xx — Session C: Verifier upgrade (question generation + Q&A evidence + Likert output)

### Changes
- `models.py`: Added `LikertScores` (4-label 1–5 int), added `questions: List[str] = []` to `AtomicClaim`, replaced `confidence: float` with `likert: LikertScores` in `VerdictResult`. Added `@property confidence` (= winning_score / 5.0) for backward compat with `main.py` / `evaluate.py`. Also `MisleadingnessResult` stub added by linter hook.
- `claim_extraction.py`: Added `_generate_questions()` — second LLM call after extraction, batches all claims into one prompt, returns 5–8 verifying questions per claim. Questions are stored in `AtomicClaim.questions`.
- `verification.py`: Rewritten prompt — verifier now receives questions + evidence, answers each question from evidence with source citation, reasons over Q&A pairs, outputs Likert scores per label. Added `_format_questions`, `_parse_likert` (with 1–5 clamp) helpers.
- `aggregation.py`: Added `_confidence(v)` helper that derives `winning_score / 5.0` from Likert; used throughout in place of `v.confidence`.

### Test results
- `uv run python3 main.py "The WHO declared monkeypox a pandemic in 2025."` — passed end-to-end, Likert visible in JSON output
- `uv run python3 evaluate.py --n 5` — passed end-to-end, no schema crashes, 2/5 accuracy (baseline, not a regression)

## 21:15 — Task 1: fix is_checkable: False bug

**Root cause:** Qwen3-32b's thinking mode overrides explicit prompt instructions. Emotional/rhetorical phrasing (e.g. "You are watching the cheaters...") classified as non-factual despite asserting voter fraud.

**Fix (prompt-level only, pipeline/claim_extraction.py):**
- Restructured `_SYSTEM`: moved is_checkable rules to top, added concrete examples covering the exact failure pattern (accusatory/emotionally framed posts)
- Added `/no_think` to both user messages — Groq's Qwen3-32b control token to disable thinking mode, preventing reasoning from overriding instructions

**Verified:** Previously failing case now returns is_checkable=true. Pure sentiment ("I love this country!") still correctly returns is_checkable=false.

## 21:30 — Task 2: 50-claim baseline evaluation (partial)

Ran `uv run python3 evaluate.py --n 50 --seed 42`. Hit daily TPD limit for llama-3.3-70b-versatile (99,350/100,000) during claim 19. 18/50 claims completed (11/18 = 61% partial). Added checkpoint support to evaluate.py; run treated as complete at 18 claims per user decision. Logged in docs/baselines.md.

## 22:00 — Session E: FIRE iterative confidence-gated retrieval

**Changes:**
- `config.py`: `MAX_RETRIEVAL_ROUNDS` 0 → 5, added `FIRE_CONFIDENCE_THRESHOLD = 4`, `FIRE_REDUNDANCY_THRESHOLD = 0.9`
- `pipeline/iterative.py` (new): `iterative_verify` wraps `verify_claim` in a loop — verify → if winning Likert ≥ 4, exit early; else generate follow-up query (LLM), check redundancy via cosine sim > 0.9, retrieve + merge evidence, repeat. Max 5 rounds.
- `evaluate.py`: swapped `verify_claim` → `iterative_verify(claim, ce, date_cutoff=..., verbose=True)`, updated step header.

**Test:** End-to-end run started (`--n 5`), retrieval completed (11 → 6 items), FIRE loop entered. Hit daily TPD limit (98,550/100,000) before first verification completed — same blocker as last session. Unit tests for `_winning_score`, `_is_redundant`, `_merge` all pass.

**Blocker:** llama-3.3-70b-versatile daily token limit. Full FIRE early-exit behavior unverified until model is swapped or quota resets.

## 21:50 — Baseline 1 logged: 11/18 = 61%

Full per-claim analysis in docs/baselines.md. Key findings:
- NEE over-prediction is the dominant failure mode (6/7 errors)
- FCTAPI hits are the strongest accuracy predictor — when there's a structured fact-check, the system almost always gets it right
- CRED-1 is nearly inert (everything gets cred=0.50)
- Wikipedia rate-limited on 5/18 claims (429 errors)
- Facebook/Twitter blocklist removes ~40% of candidate URLs
- Temporal anchoring failure on claim 7 (Trump/1994 lawsuit) — exact case IDEA-007 targets
- Verification model (llama-3.3-70b) consumed 100k daily tokens on 18 claims — model swap needed before next eval run
